# Build products
.build-melo/
outputs/

# Locally downloaded command line tools (gh). Kept out of .build-melo because
# build-app.sh wipes that folder at the start of every build.
.tools/
*.xcuserdatad
xcuserdata/

# Sparkle release staging is kept locally, not in the repo: generate_appcast
# rebuilds the feed from the archives it finds there, and they are already
# published as GitHub release assets.
outputs/sparkle-releases/

.DS_Store
_to_delete/
